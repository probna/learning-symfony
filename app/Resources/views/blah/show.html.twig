{% extends 'base.html.twig' %}

{% block body %}
    <h1>Blah</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blah.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ blah.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ blah.age }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('blah_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('blah_edit', { 'id': blah.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
